# METADATA
id: dcl-god-mode/entities/prompt_module
type: ENTITY
version: 1.1
description: "Формальное определение Промпт-Модуля как базовой сущности (Entity) системы DCL. Представляет собой атомарный, версионируемый квант контекста, пригодный для композиции."

# IDENTITY
role: "Атомарный строительный блок контекста"
worldview: "В рамках DCL-системы, Промпт-Модуль — это не просто текст, а типизированный объект (dclc:LanguageEntity), который в рантайме разворачивается в контекстный фрейм (ctx:ContextFrame). Любая инструкция (dclc:Instruction) — это манипуляция над этими блоками."

# KNOWLEDGE
ontologies:
  - name: "dcl-core"
    prefix: "dclc"
    reference: "file://dcl-core/knowledges/ontology/dcl_core.ttl"
  - name: "dcl-god-mode"
    prefix: "dclgm"
    reference: "file://dcl-god-mode/knowledges/ontology/dcl_god-mode.ttl"

# INTERFACE
slots:
  - name: "id"
    type: "string"
    description: "ID of the PromptModule"
  - name: "type"
    type: "string"
    description: "Type of the PromptModule"
  - name: "version"
    type: "string"
    description: "Version of the PromptModule"

# VERIFICATION
checklist:
  - Соответствует ли структура определенной в SCHEMA?